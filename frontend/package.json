{
  "name": "voice-rag-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild main.jsx --bundle --loader:.jsx=jsx --loader:.png=dataurl --outfile=dist/bundle.js && copy index.html dist\\index.html && copy styles.css dist\\styles.css"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "framer-motion": "^11.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}